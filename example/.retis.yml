# Example file
plugins:
  - 'https://raw.githubusercontent.com/jakhu/retis-tester-1/test/psf.cson'
name: 'retis-example'
local: true
nvm:
  - stable
language: nodejs
install: npm install
global:
  npm:
    - buildup
  gem:
    # - Gemfile: ./Gemfile
    - sass
  pip:
    - request
