// Generated by CoffeeScript 1.10.0
(function() {
  var Logger, app, fs, parseConfig;

  Logger = require('./logger').Logger;

  fs = require('fs');

  parseConfig = require('./parser').parseConfig;

  app = module.exports = {};


  /*
   * Build method
   * @param options {Object} Options
   */

  app.build = function(options) {
    this.logger = new Logger('retis', options);
    this.logger.info('Scanning for build specification...');
    if (typeof options.dir !== 'undefined') {
      this.logger.deb("CWD: " + ("\'" + options.dir + "\'").green);
    }
    parseConfig(options);
  };

}).call(this);
